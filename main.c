#include <stdio.h>
#include "stats.h"
#include "search.h"

int main(int argc, char *argv[])
{
    AdjGraph* g;
    int u;
    int v;
    FILE *fp=fopen("dummy_test.txt", "r");
    scanf("%d, %d", &u, &v);
    while(/*flag == 0 */)
    {

        fprintf(fp, "%d", dummy_test_key.txt);
    }

    return 0;
}

